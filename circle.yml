machine:
  node:
    version: 6.1.0

dependencies:
  cache_directories:
    - node_modules
  override:
    - ./circle.sh dependencies

test:
  override:
    - ./circle.sh release

deployment:
  master:
    branch: master
    commands:
      - ./circle.sh deploy
